% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimation.R
\name{wvs_profile}
\alias{wvs_profile}
\title{Compute a cultural profile for a single country}
\usage{
wvs_profile(
  country,
  wave = NULL,
  dimensions = dims_all,
  select = NULL,
  strict = FALSE,
  ci_level = 0.99,
  max_points = 5000,
  seed = NULL,
  path = NULL
)
}
\arguments{
\item{country}{Character string specifying the country name or code.}

\item{wave}{Optional numeric or character wave identifier. If NULL,
the default (most recent) wave is used where applicable.}

\item{dimensions}{A named list of dimension definitions (default
\code{dims_all}).}

\item{select}{Optional vector of variables to select before scoring.}

\item{strict}{Logical; if TRUE, use strict scoring rules when
computing dimension scores.}

\item{ci_level}{Confidence level for profile intervals in \verb{(0, 1)}.
Defaults to \code{0.99}.}

\item{max_points}{Optional maximum number of jitter points per
dimension. Set to \code{NULL} to plot all finite scores.}

\item{seed}{Optional integer seed used when downsampling points via
\code{max_points}.}

\item{path}{Optional path to a local data file; passed to
\code{wvs_data()}.}
}
\value{
An object of class \code{wvs_profile} (and \code{wvsR}) containing
\code{title}, \code{country}, \code{wave}, \code{means} (a data.frame with
\code{dimension}, \code{label}, \code{mean}, \code{lower}, \code{upper}, \code{n}), and
\code{points} (respondent-level scores for plotting). The returned
\code{points} data are used by \code{plot.wvs_profile()} to draw jittered points.
}
\description{
Compute mean scores and confidence intervals for each cultural
dimension for a single country and wave using the scoring helpers
in this package.
}
