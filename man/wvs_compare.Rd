% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimation.R
\name{wvs_compare}
\alias{wvs_compare}
\title{Compare means for two countries side-by-side}
\usage{
wvs_compare(
  countries,
  wave = NULL,
  dimensions = dims_all,
  select = NULL,
  strict = FALSE,
  ci_level = 0.95,
  max_points = 1500,
  seed = NULL,
  path = NULL
)
}
\arguments{
\item{countries}{Character vector of length 2 with country names or
codes (country1, country2).}

\item{wave}{Optional wave identifier passed to \code{wvs_data()}.}

\item{dimensions}{A named list of dimension definitions (default
\code{dims_all}).}

\item{select}{Optional variables to select before scoring.}

\item{strict}{Logical; if TRUE, use strict scoring rules.}

\item{ci_level}{Confidence level for comparison intervals in \verb{(0, 1)}.
Defaults to \code{0.95}.}

\item{max_points}{Optional maximum number of jitter points per
country-dimension. Set to \code{NULL} to plot all finite scores.}

\item{seed}{Optional integer seed used when downsampling points via
\code{max_points}.}

\item{path}{Optional path to local data; passed to \code{wvs_data()}.}
}
\value{
An object of class \code{wvs_compare} (and \code{wvsR}) containing
\code{title}, \code{countries}, \code{wave}, \code{means1}, \code{means2}, \code{points1}, and
\code{points2}. \code{means1}/\code{means2} are data.frames with \code{dimension},
\code{label}, \code{mean}, \code{lower}, \code{upper}, \code{n}. \code{points1}
and \code{points2} contain respondent-level scores used for plotting.
}
\description{
Compute mean scores and confidence intervals for each dimension
separately for two countries, returning results suitable for
side-by-side plotting.
}
