% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimation.R
\name{wvs_difference}
\alias{wvs_difference}
\title{Estimate pairwise differences between two countries}
\usage{
wvs_difference(
  countries,
  wave = NULL,
  dimensions = dims_all,
  select = NULL,
  strict = FALSE,
  ci_level = 0.95,
  path = NULL
)
}
\arguments{
\item{countries}{Character vector of length 2 with country names or
codes (country1, country2).}

\item{wave}{Optional wave identifier passed to \code{wvs_data()}.}

\item{dimensions}{A named list of dimension definitions (default
\code{dims_all}).}

\item{select}{Optional variables to select before scoring.}

\item{strict}{Logical; if TRUE, use strict scoring rules.}

\item{ci_level}{Confidence level for difference intervals in \verb{(0, 1)}.
Defaults to \code{0.95}.}

\item{path}{Optional path to local data; passed to \code{wvs_data()}.}
}
\value{
An object of class \code{wvs_difference} (and \code{wvsR}) containing
\code{title}, \code{countries}, \code{wave}, and \code{difference} (a
data.frame with \code{dimension}, \code{label}, \code{diff}, \code{lower}, \code{upper}, \code{d}).
}
\description{
Compute mean differences (country1 − country2) and 95\% confidence
intervals for each cultural dimension between two countries.
}
